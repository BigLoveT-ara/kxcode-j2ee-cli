# 项目技术栈
[这里填充项目技术栈内容]


<% if (projectType === 'ptah') { %>
# 项目介绍

## 人机交互系统外部接口调用
人机交互系统会在需要时调用本项目接口，接口中携带历史人机交互信息、上下文变量信息等，当你需要更新人机交互系统的上下文变量信息时可以通过返回特定结构来实现。
机器人系统会使用上下文变量信息来实现动态回复内容的拼装或者是人机交互流程的走向判断。
Api 接口的入参与返回参数均已设计完成，请参考`docs/framework/brain-session.md`进行开发。

## 人机交互系统数据推送处理
人机交互系统会在会话结束后推送完整的人机交互信息到Kafka，包括历史人机交互信息、上下文变量、标签信息等。
项目可以通过监听kafka的相关Topic实现数据的监听和异步处理。
Kafka中存放的数据结构已经确定，请参考`docs/framework/brain-session.md`

## 人机交互信息的核心字段
    人机交互信息已数组形式存放，每次人机交互都会在数组中已一个对象的形式存在。
    对象中包含机器人播报节点名称、用户回答命中节点名称、用户回答内容、命中业务标签等。

## 上下文变量信息
    上下文变量信息已key-value键值对的形式存放，仅支持Integer、String等基本数据类型，key采用英文命名防止出现歧义。
    同名（key相同）的上下文变量信息
<% } -%>

# 项目包结构
```
[这里填充项目包结构内容]
```

# 项目相关文档
表结构设计文档、原始需求文档存储在 `docs`下，在需要查看的时候请使用`Read`进行读取
- `docs/design`下存放的是工程相关设计文档如产品需求文档、数据库设计文档
- `docs/framework`下存放的是框架相关 Api 使用方面的文档如 mybatisplus-method.md,utils.md

# 工具类的使用
需要使用到字符串、文件、集合、编解码、加解密相关的 Api 时请查阅`docs/framework/utils-method.md`文档

## Spring注入规范
- 优先使用final定义变量与`@RequiredArgsConstructor`注解配合使用实现注入


<% if (projectType === 'ptah') { %>
# 业务包标准接口功能描述
[这里填充业务包对外提供标准输入输出接口的描述]
<% } -%>

# 其他核心设计内容

## 消息队列
[这里填充消息队列相关内容]

## 定时任务
[这里填充定时任务相关内容]

## 高速缓存
[这里填充定时任务相关内容]

# 数据库表设计和核心关系

## 数据库表与 DAO 映射
[这里填充项目DAO对象与表的映射内容]

## 表关联
[这里填充表结构之间的关系]
